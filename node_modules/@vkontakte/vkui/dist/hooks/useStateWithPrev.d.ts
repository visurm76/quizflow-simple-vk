import * as React from 'react';
/**
 * Возвращает значение с текущим и предыдущим состоянием
 *
 * # Пример
 *
 * ```ts
 * const [[count, prevCount], setCount] = useStateWithPrev(initialState);
 * ```
 */
export declare function useStateWithPrev<T>(initialState: T | (() => T)): [[T, T | undefined], React.Dispatch<React.SetStateAction<T>>];
//# sourceMappingURL=useStateWithPrev.d.ts.map