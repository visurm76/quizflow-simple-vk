import * as React from 'react';
import type { HasRootRef } from '../../types';
export interface ScrollArrowProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, HasRootRef<HTMLButtonElement> {
    /**
     * Направление стрелки.
     */
    direction: 'up' | 'right' | 'down' | 'left';
    /**
     * Размер стрелки.
     */
    size?: 's' | 'm';
    /**
     * Смещает иконку кнопки навигации по вертикали.
     */
    offsetY?: number | string;
    /**
     * [a11y]: Используется для ассистивных технологий.
     */
    label?: string;
}
/**
 * Компонент стрелки. Используется в [HorizontalScroll](#/HorizontalScroll) и [Gallery](#/Gallery).
 *
 * @since 5.4.0
 * @see https://vkui.io/components/scroll-arrow
 */
export declare const ScrollArrow: ({ size, offsetY, direction, label: labelProp, children, ...restProps }: ScrollArrowProps) => React.ReactNode;
//# sourceMappingURL=ScrollArrow.d.ts.map