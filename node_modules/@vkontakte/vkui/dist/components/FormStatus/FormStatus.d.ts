import * as React from 'react';
import type { HTMLAttributesWithRootRef } from '../../types';
export interface FormStatusProps extends Omit<HTMLAttributesWithRootRef<HTMLDivElement>, 'title'> {
    /**
     * Режим отображения компонента.
     */
    mode?: 'default' | 'error';
    /**
     * Основной текст.
     */
    title?: React.ReactNode;
}
/**
 * @see https://vkui.io/components/form-status
 */
export declare const FormStatus: ({ mode, children, className, role, title, ...restProps }: FormStatusProps) => React.ReactNode;
//# sourceMappingURL=FormStatus.d.ts.map